{
  "name": "modgarage",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "postinstall": "cd frontend && npm install --legacy-peer-deps",
    "start": "cd frontend && npm start",
    "build": "cd frontend && npm install ajv-formats && npm run build"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  }
}